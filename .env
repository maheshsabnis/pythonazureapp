# Use either a full connection string OR the individual parts (comment out the unused block)

# Full connection string (recommended for simplicity)
# Example includes recommended encryption options for Azure SQL
SQL_CONNECTION_STRING="Driver={ODBC Driver 17 for SQL Server};Server=tcp:ccccc.database.windows.net,1433;Database=EComm;Uid=user1@vvvvv;Pwd=mypwd;Encrypt=yes;TrustServerCertificate=no;Connection Timeout=30;"

# OR: separate connection parameters (uncomment and set if not using SQL_CONNECTION_STRING)
# SQL_SERVER can be just the server name or full host like YOUR_SERVER.database.windows.net
SQL_SERVER=cccccc.database.windows.net,1433
SQL_DATABASE=ECOMM
SQL_USER=user@cccccc
SQL_PASSWORD=my_secure_password
# Optional: change driver to match installed ODBC driver on the machine
SQL_DRIVER="ODBC Driver 17 for SQL Server"

# Optional app settings
AZURE_FUNCTIONS_ENVIRONMENT=Development
LOG_LEVEL=INFO
#SQL_PORT=1433

